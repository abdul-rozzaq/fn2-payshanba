<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        fieldset {
            width: fit-content;
        }

        label {
            display: inline-block;
            min-width: 100px;
        }

        input {
            padding: 4px;
        }

        .field {
            margin-bottom: 5px;
        }

        button {
            width: 100%;
            padding: 5px;
        }
    </style>
</head>

<body>

    <form method="POST">
        <fieldset>
            <legend>Ro'yhatdan o'tish</legend>
            <div class="field">
                <label for="username">Username</label>
                <input type="text" name="username" placeholder="john_doe" id="username" value="abdurazzoq">
            </div>
            <div class="field">
                <label for="email">Email</label>
                <input type="email" name="email" placeholder="example@gmail.com" id="email" value="a.abdurazzoq@internet.ru">
            </div>
            <div class="field">
                <label for="password">Parol</label>
                <input type="text" name="password" placeholder="* * * * * * * *" id="password" value="123456">
            </div>
            <button>Yuborish</button>
        </fieldset>
    </form>

    {{#if message}}

    <h3>Message: {{ message }}</h3>

    {{/if}}

</body>

</html>